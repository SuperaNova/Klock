<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/alarmCard"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="8dp"
    android:layout_marginVertical="4dp"
    app:cardElevation="2dp"
    app:cardCornerRadius="8dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Collapsed Section -->
        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/collapsedContentGroup"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:padding="16dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <!-- Alarm Time (e.g., 8:30) -->
            <TextView
                android:id="@+id/alarm_time"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textAppearance="?attr/textAppearanceHeadlineMedium"
                android:textSize="32sp"
                android:textStyle="bold"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                tools:text="8:30" />

            <!-- AM/PM -->
            <TextView
                android:id="@+id/alarm_am_pm"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textAppearance="?attr/textAppearanceBodyMedium"
                android:textSize="14sp"
                android:layout_marginStart="4dp"
                app:layout_constraintStart_toEndOf="@id/alarm_time"
                app:layout_constraintBaseline_toBaselineOf="@id/alarm_time"
                tools:text="AM" />

            <!-- Alarm Label / Repeat Days (single line when collapsed) -->
            <TextView
                android:id="@+id/alarm_label_repeat_info"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:textAppearance="?attr/textAppearanceBodyMedium"
                android:textSize="16sp"
                android:textColor="?android:attr/textColorSecondary"
                android:maxLines="1"
                android:ellipsize="end"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/alarm_time"
                app:layout_constraintEnd_toStartOf="@id/alarm_enabled_switch"
                app:layout_constraintBottom_toBottomOf="parent"
                tools:text="Work Alarm, Mon, Tue, Wed" />

            <!-- Expand/Collapse Icon -->
             <ImageView
                 android:id="@+id/expand_icon"
                 android:layout_width="wrap_content"
                 android:layout_height="wrap_content"
                 android:src="@drawable/ic_arrow_down_24" 
                 android:contentDescription="Expand alarm details"
                 app:tint="?android:attr/textColorSecondary"
                 app:layout_constraintBottom_toBottomOf="@id/alarm_label_repeat_info" 
                 app:layout_constraintEnd_toStartOf="@id/alarm_enabled_switch"
                 android:layout_marginEnd="8dp"/>

            <!-- Enabled Switch -->
            <com.google.android.material.switchmaterial.SwitchMaterial
                android:id="@+id/alarm_enabled_switch"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"/>

        </androidx.constraintlayout.widget.ConstraintLayout>

        <!-- Divider -->
        <View
            android:id="@+id/divider"
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:background="?android:attr/listDivider"
            android:visibility="gone"
            app:layout_constraintTop_toBottomOf="@id/collapsedContentGroup"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            tools:visibility="visible" />

        <!-- Expanded Section -->
        <LinearLayout
            android:id="@+id/expandedContentGroup"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:paddingBottom="16dp"
            android:visibility="gone"
            app:layout_constraintTop_toBottomOf="@id/divider"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            tools:visibility="visible">

            <!-- Repeat Checkbox & Days -->
            <CheckBox
                android:id="@+id/repeatCheckbox"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Repeat"/>

            <com.google.android.material.button.MaterialButtonToggleGroup
                android:id="@+id/toggle_button_group"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:selectionRequired="false"
                app:singleSelection="false">
                <!-- Buttons S, M, T, W, T, F, S -->
                 <Button style="@style/Widget.App.MaterialButton.Toggle" android:id="@+id/button_sunday" android:layout_width="0dp" android:layout_height="wrap_content" android:layout_weight="1" android:text="S"/>
                 <Button style="@style/Widget.App.MaterialButton.Toggle" android:id="@+id/button_monday" android:layout_width="0dp" android:layout_height="wrap_content" android:layout_weight="1" android:text="M"/>
                 <Button style="@style/Widget.App.MaterialButton.Toggle" android:id="@+id/button_tuesday" android:layout_width="0dp" android:layout_height="wrap_content" android:layout_weight="1" android:text="T"/>
                 <Button style="@style/Widget.App.MaterialButton.Toggle" android:id="@+id/button_wednesday" android:layout_width="0dp" android:layout_height="wrap_content" android:layout_weight="1" android:text="W"/>
                 <Button style="@style/Widget.App.MaterialButton.Toggle" android:id="@+id/button_thursday" android:layout_width="0dp" android:layout_height="wrap_content" android:layout_weight="1" android:text="T"/>
                 <Button style="@style/Widget.App.MaterialButton.Toggle" android:id="@+id/button_friday" android:layout_width="0dp" android:layout_height="wrap_content" android:layout_weight="1" android:text="F"/>
                 <Button style="@style/Widget.App.MaterialButton.Toggle" android:id="@+id/button_saturday" android:layout_width="0dp" android:layout_height="wrap_content" android:layout_weight="1" android:text="S"/>
            </com.google.android.material.button.MaterialButtonToggleGroup>

            <!-- Ringtone -->
            <TextView
                android:id="@+id/ringtoneText"
                style="?android:attr/listSeparatorTextViewStyle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Default (Sound Name)"
                android:drawablePadding="8dp"
                app:drawableStartCompat="@drawable/ic_notifications_black_24dp" />

            <!-- Vibrate -->
            <com.google.android.material.checkbox.MaterialCheckBox
                android:id="@+id/vibrateCheckbox"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Vibrate"/>

            <!-- Label -->
             <EditText
                 android:id="@+id/alarmLabelEditText"
                 android:layout_width="match_parent"
                 android:layout_height="wrap_content"
                 android:hint="Label"
                 android:inputType="textCapSentences"
                 android:background="@null" 
                 android:paddingTop="16dp"
                 android:paddingBottom="16dp"
                 app:drawableStartCompat="@drawable/ic_label_black_24dp"
                 android:drawablePadding="8dp"/>

            <!-- Delete Button -->
            <Button
                android:id="@+id/deleteButton"
                style="@style/Widget.MaterialComponents.Button.TextButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="start"
                android:text="Delete"
                android:textColor="?attr/colorError"
                app:icon="@android:drawable/ic_menu_delete"
                app:iconTint="?attr/colorError" />

        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>
